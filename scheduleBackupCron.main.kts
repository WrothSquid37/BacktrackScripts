/*
 * This script is meant meant to find and edit the cron job file to schedule the backup.
 * It is used to backup the database and the files.
 * It is used to backup the database and the files.
 * 
*/
